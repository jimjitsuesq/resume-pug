extends layout.pug

block body-content
  main#resume.page
    +resume-header
    +summary
    .resume-content 
      aside.left-column
        +about
        +skills
        +interests
      .right-column 
        +education
        +work
